apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "myprod.fullname" . }}-test-connection"
  labels:
    {{- include "myprod.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "myprod.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
