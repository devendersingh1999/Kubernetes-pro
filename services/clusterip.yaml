apiVersion: v1
kind: Service
metadata:
  name: web-cluster
  labels:
    app: nginx-cls

spec:
  type: ClusterIP
  selector:
    app: nginx

  ports:
  - port: 80 #service port
    targetPort: 80 #container port expose
